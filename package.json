{
  "name": "wallet-webhook-example",
  "version": "1.1.0",
  "description": "Example implementation of webhook processing order, calculating points and adding to user's wallet",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "start:func": "nodemon --watch dist --delay 1 --exec \"func start -p 7072\"",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "axios": "^0.27.2",
    "crypto": "^1.0.1",
    "dayjs": "^1.11.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^17.0.38",
    "typescript": "^4.0.0"
  }
}
